# =============================================
# PROFIL PRODUCTION - SIMULATION AVEC H2
# =============================================

# 🗄 DATABASE - H2 EN MODE MYSQL (SIMULATION)
spring.datasource.url=jdbc:h2:mem:prod_db;MODE=MySQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=prod_user
spring.datasource.password=prod_password_123

spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

# 🚀 PORTS PRODUCTION DIFFÉRENTS
# (Les ports spécifiques seront définis dans chaque service)

# 📊 MONITORING - PRODUCTION (sécurisé)
management.endpoints.web.exposure.include=health,info,metrics
management.endpoint.health.show-details=when_authorized

# 🔧 RESILIENCE4J - PLUS AGGRESSIF EN PROD
resilience4j.circuitbreaker.configs.default.wait-duration-in-open-state=2s
resilience4j.timelimiter.configs.default.timeout-duration=1s

# 📝 LOGGING - PRODUCTION (moins verbeux)
logging.level.com.example=WARN
logging.level.org.springframework=WARN
logging.level.org.hibernate=ERROR

# 🌐 NETWORK - PRODUCTION
eureka.client.service-url.defaultZone=http://localhost:8761/eureka

# 🔐 SECURITY - PRODUCTION
spring.security.user.name=prod_admin
spring.security.user.password=strong_prod_password_456